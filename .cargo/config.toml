# 产物固定在仓库根目录下的 target/（workspace 默认）。
# 若环境变量 CARGO_TARGET_DIR 被设置（例如 Cursor 沙箱），会覆盖此处；
# 请使用 npm 脚本 tauri:build* / tauri:dev / dev:core，由 scripts/run_without_cargo_target_dir.mjs 清除该变量。
[build]
target-dir = "target"
